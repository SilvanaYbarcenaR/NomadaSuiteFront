const initialState = () => { };

const rootReducer = (state = initialState, { type, payload }) => {
    switch (type) {
        // case value:
                
        default:
            return {
                ...state
            }
    }
};

export default rootReducer;